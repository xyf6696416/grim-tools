# 2026-09-22

## 10:07~ 武器类型门控 + 星座数据补洞（用户：「星座构建有问题：狂战士需要斧或矛、苦难无收益」）

### ★ 关键前置：**玩家在游戏里换了武器套**（指纹 `2e8dc006a055` → `ddb54cc16610`）
| 套 | 主手 | 副手 | 类型 |
|---|---|---|---|
| **alt1（现在在用）** | 刻罪者 `d302_sword` | 血肉盛宴 `c018_axe` | 剑 + **斧** |
| alt2 | 扭曲精神 `d004_sword` | 蝎尾狮长剑 `b008c_sword` | 剑 + 剑 |

swap 后**减抗 C 族 −30% 整条消失**（那条来自 alt2 主手「扭曲精神」的底材）。
⇒ 锚点必须重设 **v14**。面板 83,536.1 → **81,363**。

### ⚠ 一个必须记住的教训：**判据不一致的报告可能是「存档变了」而不是 bug**
上一轮我看到 `alt1_unused=True / alt2_unused=False / use_alt_weaponset=True`，
据此报「`_active_weapon_set`(alt1) vs `gd dps`(alt2) vs `base_gids`(alt1) 三处不一致」。
本次复查存档变成 `alt1_unused=False / alt2_unused=True / use_alt_weaponset=False`
⇒ **三处判据现在全部自洽（都给 alt1）**。
**下次遇到「三处判据互相矛盾」，先复算指纹确认存档没动，再报 bug。**

### 陷阱 #80：星座星位**整类**拿不到补洞字段（一个 tag 对多条记录）
- `tools/extract_calc_skills.py` 的映射靠 `skillDisplayName` tag ↔ `skills.json` 的 `tag`，
  且规则是「候选唯一才采用」。
- 而星座星位是**一个 tag 对多条**：`tagDevotion_B25`（狂战士）→ `tier2_25a~f` **六条**
  ⇒ 旧逻辑一律判「歧义跳过」⇒ **星座星位整类没有补洞字段**。
- 后果：技能的**武器类型白名单**（`Axe` / `Axe2h` / `Spear2h` / `Sword` / `Mace` / `Shield` /
  `Dagger` / `Scepter`）在库里**全库为 0 条**，模型完全不知道「需要斧或矛」这种硬前提。
- 修法：加**数值指纹消歧** —— 共同字段 ≥2 且逐值相等，再按**字段集对称差**排序取最小者
  （`tier2_25b` 的 `defensiveFreeze` vs `tier2_25d` 的 `defensiveStun` 公共字段完全一样，
  只靠「共同字段相等」会并列 ⇒ 必须再比字段集）。
- 结果：产物 **344 → 749 条**（+405 条靠指纹救回）、+2218 字段值、
  **52 条记录带武器限制**。

### 陷阱 #81：武器类型门控必须放在**加成收集之前**
- 新增 `gd/procs.weapon_type_ok(entry, st)`（记录顶层武器键是 **OR** 语义：
  「斧或矛」= 有斧**或**有矛）+ `final_report(weapon_st=…)`。
- **踩坑**：第一版把门控放在 `_recs = form_gate(...)` 旁边（约 1427 行），
  只过滤技能循环 ⇒ **面板一位不变**（83,536 → 83,536）。
  因为星位的**被动加成**在更早的 `for rec, lv in (levels or {}).items()`（约 1181 行）
  就已经折进 `pct` / `sk_pct` 了。
- **正确位置 = 函数开头、加成收集之前**；两边（`levels` + `_recs`）都要剔。
- `weapon_st=None` ⇒ 整段跳过、**零漂移**。

### 现状（Sam，alt1 = 剑 + 斧）
- 狂战士 `tier2_25a/d/e`（需 `Axe`/`Axe2h`/`Spear2h`）⇒ **有斧 ⇒ 限制满足、保留**。
  **门控剔除 0 条** —— 上一轮「3 点全废」的结论**作废**（那是基于 alt2 双剑的旧状态）。
- 苦难 `tier2_16a` = +40% 活力衰减 +40% 毒素持续 ⇒ 对物理/穿刺/流血 BD **零收益** ✅ 用户判断正确。

### 锚点 v14
面板 **81,363** ｜ 实战 98,873 ｜ 含减抗 163,959 ｜ OA 2,092.2 ｜ DA 1,404.5 ｜
PTH 115.32 ｜ 暴击 22.82% ｜ 期望 1.2152 ｜ 指纹 **`ddb54cc16610`**。
`selftest` 两处版本指针 → v14；**全绿**；`rerun_final_docs` 已重算 12 份 CYCLE + REPORT。

### 仍未做完
1. `gd dps` **CLI 路径**还没接 `weapon_st`（只有 `plan_dps.dps_of` 接了）。
2. 数据补洞 +2218 字段值的**副作用未逐一评估**（「常驻类技能 3 → 2」已确认是换武器套
   导致镶嵌变更所致，非 bug）。
3. `rr_items.py` 尚未纳入武器限制字段清单（现在只覆盖减抗三族）。

## 10:13~ 武器门控铺到全部调用点 + 补洞副作用审计（用户：「是的补上修复」）

### ① 上轮欠的「CLI 路径没接 weapon_st」已补，而且**远不止 CLI**
实测漏接的是 **9 条路径**（只有 `plan_dps.dps_of` 接了）：
`gd dps` CLI ／ `tune_devotion` ／ `tune_skills` ／ `tune_attrs` ／
`eval_build_variants` ／ `gt_regress`（×2）／ `defense_audit` ／ `gd.gen` ／ `gd.mana`
⇒ **同一角色在不同工具里出两个数，且无任何提示**（陷阱 **#82**）。

三层修法：
1. **单一真源** —— `gd.dps.load_char()` 源头算 `weapon_st` 存进返回字典。
2. **统一入口** —— `gd.procs.weapon_state_of(char)`（优先吃存值，取不到再推）。
   `weapon_state()` 加**记忆化**（按 `tuple(gids)`，返回副本）—— 它现在在热路径上。
3. **★★ AST 静态守卫** —— selftest `[61]` 强制**每个** `final_report(` 调用点点名
   `weapon_st`（现 **19 处**全部已接）。这条防「以后新加的调用点又忘了接」。

### ② 挖出**第二条通道也漏了门控**（陷阱 #83，比 CLI 更要命）
`final_report` 的加成有**两个入口**：`levels`（存档/形态）与
**`devotion_levels`（显式星座通道）**。门控第一版只遍历 `levels` ⇒
**`tune_devotion` / `eval_build_variants` 完全不受武器限制约束**（星座优化器就在这条路上）。
现两条都过门，账本 `weapon_dropped` 带 `channel` 标注（`levels` / `devotion`）。
★ 验证技巧：星座通道看 **`rep['dev_nodes']`**（双剑 0 / 剑+斧 1 / 不传 1），
比「比面板数字」干净 —— 合成用例下面板恒为 0，比不出差异。

### ③ CLI 报告**可见性**
`gd dps` 报告在门控账本非空时**明说**「N 条因武器不符完全不生效」+ 逐条给原因；
空账本不占版面。防的就是「玩家只看到数字变小、不知道哪条为什么」。

### ④ 补洞 +2218 字段值的副作用 —— **已审计，良性**
- 结构上：**纯增量**。405 条新记录（404 星座星位 + 1 专精），
  **0 条已有记录被改值**（逐字段 diff）。
- 新增字段只有三类：`skillMaxLevel` ×405（渲染/签名用，`_SG_IGN` 已忽略）、
  **武器限制键**（`Axe2h` 26 ／ `Mace2h` 26 ／ `Spear2h` 26 ／ `Sword2h` 22 ／
  `Shield` 13 ／ `Mace` 11 ／ `Axe` 11 ／ `Sword` 7 ／ `Dagger` 7 ／ `Scepter` 7；
  规范判据下共 **46 条记录**带限制）、`retaliation*`（约 30，模型本来就不含报复）。
- **A/B 实测**：临时换回旧文件跑 Sam ⇒ 面板 **81,363 / 含减抗 163,959**，
  与新版**逐位相同**。⇒ 上轮怀疑的「补洞把面板从 83,536 打到 73,201」
  **与补洞无关**，纯粹是换武器套 + 当时门控半成品所致。**更正完毕。**
- ⚠ 一处口径修正：上轮日报/文档写的「52 条带武器限制」是**宽口径**（含 `Offhand`/
  `Ranged*`）；规范键集（`WEAPON_TYPE_CLASS`）下是 **46 条**。文档已对齐。

### ⑤ 自检 `[61]` 共 15 条（全绿）
AST 守卫 ／ `load_char` 存值与现推一致 ／ `weapon_state_of` 两路一致 ／ 记忆化幂等 ／
**两条通道都过门** ／ `weapon_st=None` 零漂移 ／ 数据层狂战士六星限制 ／
数据层限制字段可读 ／ 中文原因可读 ／ **CLI 渲染分支（有/无账本两态）**。

### ⑥ 现状
锚点 **v14 不变**（面板 81,363 ｜ 含减抗 163,959 ｜ 指纹 `ddb54cc16610` 未动）——
只改模型接线、数字逐位一致 ⇒ **不升版本**，note 扩写记录本次范围。
`rerun_final_docs` 已重算 12 份 CYCLE + REPORT；`sync_live` 全部一致；`selftest` 全绿。

## 10:34~ 「根据当前存档还有上升空间吗」→ 挖出陷阱 #84（口径 bug，比空间本身更重要）

### 结论：**有空间，而且「说没空间」的那两个工具本身口径是错的**

| 轴 | 口径可信度 | 实测余量 |
|---|---|---|
| 装备（含武器） | ✅ `plan_dps`（含减抗） | **≥ +9.8%**（见下） |
| 星座 | ❌ 面板口径 ⇒ 至少漏算减抗/OA | 自报 **+4.57%**（下限） |
| 技能 | ❌ 面板口径 ⇒ **整张表不可信** | **未知**，可能很大 |
| 属性点 | ✅ 已修 shim | **0%**（全枚举证明 10/73/2 全局最优） |

### ① 存档有 **2 个非法装备位**（`gd auto` 起点就报了）
- 主手/副手**组件** = `it2860` = **「弹性铠甲片」**（`compa_resilientchestplate`），
  合法槽位按数据是 `c11/c12/c26/c27`（**肩/胸/法器/盾**）⇒ **装进武器非法**。
- 槽位码表已用数据反推核实：`c10–c16` 护甲（头肩胸手腰腿脚）／`c20–c25` 单手武器
  （剑斧锤枪匕杖）／`c26` 法器副手／`c27` 盾／`c28–c32` 双手武器／
  `c40` 戒指 `c41` 项链 `c42` 勋章 `c43` 圣物 `c44` 附魔 `c45` 镶嵌物。
- 存档的 alt2（未启用）里装的才是 **`compa_viciousspikes`「恶毒尖刺」**（武器合法）。
- **扫 43 个武器合法镶嵌物**（单换主手）：最好 `it8898`「**刀刃之印**」(`component_b02_sealofblades`,
  ilvl 75／Magical，+10 穿刺平伤 +50% 穿刺 +5% 生命吸取 + `sk1379`)；
  恶毒尖刺 +4.62%。**双槽都换**：刀刃之印×2 = **+9.82%**（163,959 → 180,067）；
  恶毒尖刺×2 = +5.73%；刀刃之印+恶毒尖刺 = +9.59%。

### ② ★★★ 陷阱 #84：`objfunc.score` 的口径取决于 `rep` 来自哪个函数
- `plan_dps.dps_of()` 的 `rep['dps']` = **`dps_vs`（含减抗）**；
  `gd.rotation.final_report()` 的 `rep['dps']` = **面板**（不含命中/暴击/敌抗/护甲），
  含减抗在 `rep['vs']['dps_vs']`。
- ⇒ `tune_skills` / `tune_devotion` / `eval_build_variants` / `gt_regress`
  **四处仍是面板口径**（`tune_attrs` 已修 + 自检 `[58]` 守着；装备轴本来就对）。
- **实测后果**（Sam，拆掉技能）：

| 技能 | 工具报（面板） | 真实（含减抗） |
|---|---|---|
| **刺骨战吼** | **0** | **−24.36%** |
| 血莽 `werewolf3` | 0 | −4.72% |
| 集结战吼 `rallyingcry1` | 0 | −2.99% |
| 不羁狂怒 `passive03` | 0 | −2.97% |
| 刀灵（宠物） | 0 | 0（**唯一真的 0**） |

  ⇒ 技能轴会**主动建议退掉全角色最值钱的减抗技能**。方向最危险的一类错误：
  **把最大收益项当废物**。
- 修法：照 `tune_attrs` 的 shim 再包一层（`shim['dps'] = rep['vs']['dps_vs']`）。
  ⚠ 属「重写级」改动（三轴目标全变、结论与交付物全变）⇒ **待用户批准**。

### ③ 顺带核实的两件小事
- `bloodborne1` 是**血源苏醒**（值 551/点），**不是**我说的「血莽」；血莽 = `werewolf3`。
  （第一次把名字认错了，已更正。）
- `tune_devotion --marginal`：**苦难 1 点 + 铁锤 3 点 = 拆掉损失 0.00%**（真·零收益，与上轮一致）；
  `--realloc` 退这 4 点 → 补 **九头蛇 3 点（+4.04%）+ 螳螂 1 点（+0.51%）** = **+4.57%**。
- 道具技能/宠物：`summon_bladespirit`（刀灵）确实是模型边界（宠物不在模型内）。

### ④ `--extreme` 全链路搜索结果（413 秒，32 链）
- 现状 **163,959** → 搜索 **176,235（+7.5%）**，14 槽改了 **13 个**，
  **满抗 1070/1070 全封顶**（穿刺 144/需 130 等）；
  两个非法组件被换成 **恶毒尖刺 `it2878`**（搜索自己发现并修了）。
- ★ **但搜索漏了一手**：在它的方案上把组件换成 **「刀刃之印 `it8898`」×2 ⇒ 再 +3.65%**
  （176,235 → **182,663**）。两组件都不给抗性 ⇒ 换掉不影响满抗。
  怀疑组件候选池被 `--comp-topn 32` / `--dedup 6` 截掉 ⇒ 下一轮调大再跑。
- **最终推荐（已落 `data/plans/pivot/headroom_best.json`）**：
  面板 **90,433** ｜ 实战 **109,444** ｜ 含减抗 **182,663** ｜ 相对存档 **+11.41%** ｜
  `plan_legal` **0 处非法**。
- ⚠ `gd auto` 报表里「评估次数（含缓存命中）：2」看着偏小，LNS 的实际搜索量待核。
- 交付：`data/plans/final/HEADROOM_Sam.md`（四轴余量盘点 + 陷阱 #84 证据）。

## 11:00~ 修口径（用户：「落档先不急先修」）—— 陷阱 #84 + #85 一并修掉

### ① #84 修法：**只改一处** —— `tools/objfunc.py` 的 `total` 分支
```python
if m == 'total':
    s = _total_vs(rep)      # ← 原来是 float(rep['dps'])
```
- `_total_vs()` **文件里本来就有**（给 `bucket` 的 `+` 变体用的），兼容两种入参：
  先看 `rep['vs']['dps_vs']`，再看 `rep['dps_vs']`，最后才 `rep['dps']`。
- 对 `dps_of()` 的返回**逐位不变**（它的 `dps` 本就 == `dps_vs`，实测 163,959 ↔ 163,959）
  ⇒ **装备轴零漂移**，锚点 v14 不动、`rerun_final_docs` 数字全同。
- 对 `final_report()` 的返回自动取实战层。
- ★ 一处修掉**全部**走 objfunc 的工具（tune_skills / tune_devotion / eval_build_variants）。
- ⚠ **更正**：`gt_regress` **不经过 objfunc**（它刻意取面板去对拍 GT 网站，容差 2%）
  —— 我上一轮把它列进受影响名单是写错了，已在 pitfalls 里更正。

### ② #85（修 #84 之后才暴露出来的第二个）：减抗被冻结
- 三轴都在**模块级**算一次 `RR.collect_char(...)` 塞进固定 kwargs
  ⇒ 改减抗技能/星位的等级**永不见效**。
- ★ **它藏起来的原因**：RR 只作用于 `vs` 层，**面板不含 RR** ⇒ #84 没修前根本看不见。
  #84 与 #85 是**同一对**。
- 数据：刺骨战吼 `defensivePierce` 逐级 `[-4,-7,…,-30,…]`（22 级）
  ⇒ lv1 = 12% ／ lv11 = 36% ／ lv12 = 38%（`collect_char` 实测）。
- 修法：`gd/rr.py` 新增 **`RRCache`** —— 只把「带减抗字段的记录」的等级拼成 key，
  key 变才重算，其余命中缓存（**同速**）。`extra=` 接第二条通道（`devotion_levels`，
  星座星位里也有 B 族减抗「暗杀者的标记 −8」）。
  三处改 `rr=_RRC.pack(eff[, dev])`；**`tune_attrs` 不用改**（属性点不影响减抗）。

### ③ 修完的**新结论**（这才是真实数字）
| 项 | 修前 | **修后** |
|---|---|---|
| 技能轴（换位收敛） | 面板 +0.16% | **评分 +1.26%**（163,959 → 166,032；血源苏醒 −3 / 贪噬 +3） |
| 刺骨战吼 拆 1 点 | **0** | **−1.78%**（2,917 分） |
| 不羁狂怒 拆 1 点 | **0** | −0.57% |
| 星座重排 | 面板口径 89,031 | 评分口径 **148,519 → 155,308（+4.57%）**（推荐不变） |
| 星座 苦难/铁锤 | 0 | **仍 0**（实战层也确实是零收益，结论稳） |
| 装备轴 | 含减抗 | **不变**（零漂移） |
★ **面板口径的系统性低估 ≈ 8×** ⇒ 以后这三轴**只认「评分」列**。

### ④ 顺手修的显示问题（防误读）
- `tune_skills` 边际表标题「拆点损失最小」实际打印的是**最大**的一批 ⇒ 改为
  「拆点损失**最大**（最不该拆；越靠下越便宜）」。
- 轮次 / 单步 / 换位 / 结果四处输出**改为评分优先**（面板作附注）——
  否则用户会以为「优化只值 +0.16%」。

### ⑤ 自检 `[62]` 共 9 条（全绿）
objfunc(total) 取实战层（合成 rep 守卫）／桶目标未被波及 ／ **tune_skills 基线评分
== `dps_of` 的含减抗值（163,959.4 vs 163,959）** ／ **减抗技能拆 1 点必须有损失
（刺骨战吼 2,916.7）** ／ RRCache 与直算逐位一致 ／ 幂等 ／ 动无减抗技能不重算
（性能守卫）／ 动减抗技能必重算（pierce 38→12）／ **三轴都用 RRCache 的回退守卫**。
`selftest` **全绿**；`sync_live` 全部一致；`rerun_final_docs` 12 份产物数字全同。

## 11:00~ 补跑装备搜索（用户要求调大 `--comp-topn`）→ 结论翻转，撤回一个错误推荐

### ★★★ 最重要的一条：**我上一条「刀刃之印 +3.65%／+11.41%」是错的，已撤回**
- GT 库真源（`data/cache/itemdb.js` 的键字典）：
  ```js
  levelRequirement: "k"      ← **等级要求**
  Class:            "l"      ← **槽位类别码**（`WeaponMelee_Sword:"c20"` ⇒ c20 = 剑）
  ```
- **「刀刃之印」`it8898` 的 `k` = 75 > 角色 73 级 ⇒ 游戏里装不上。**
- 我错在：① 把 `k` 当「物品等级」（它俩对镶嵌物并不相等：恶毒尖刺 k=15／itemLevel=20）；
  ② 更早还差点信了 `_req_lvl` 的注释（把 `l` 当等级）。
- 佐证：存档实际在穿的 14 件 `k` 全部 ≤ 70（角色 73）—— `k` 就是等级要求。
- ⇒ 作废方案 `headroom_best.json` **已删除**；正确结论 = `--extreme` 搜索自己给的数。

### ① 修掉两个真实缺陷
1. **`gd/opt.py::_req_lvl()` 语义错**（陷阱 #86）：旧实现把 `l` 用 `c(\d+)` 解析当等级
   ⇒ 恒返回 10~50 的「槽位码数字」，对 `_MAX_ILVL` 的过滤**永远放行**、等级闸门形同虚设
   （实测 `it729`：旧 20 ／ 真实 `k`=50）。改为直接读 `k`（回退 `itemLevel`）。
   **修后 `POOL_BASE`/`POOL_WPN` 规模不变（778/6）⇒ 当前等级零漂移。**
2. **`tools/plan_legal.py` 只判槽位、不判等级**（陷阱 #86 第 2 处）：
   新增 `check_level()` + `--level` / `--char`（不传则**醒目警告**，不静默跳过 —— #82 的形态）。
   用它一跑就抓出 `headroom_best.json` 的 **2 处超等级**。

### ② 真正导致「搜索没找到好组件」的原因（陷阱 #87）—— `--comp-topn` **不是瓶颈**
- 武器槽候选池原先只有**硬编码两项** `POOL_CWPN = ['it2878','it2849']`。
- 通用 `auto_comp_pool()` 在 **`--goal super`** 下按 `_pool_score()`（= **抗性总点**）排序，
  且 `if sc > 0` **把零抗性的纯输出件整批丢掉** ⇒ 实测 `auto_comp_pool(32)`
  给「主手」只剩 **1 项**。⇒ 调大 `--comp-topn` 只会多加「抗性件」，加不进输出件。
- 修法：新增 `gd/opt.py::auto_wpn_comp_pool()`（等级合法 + 槽位合法 + 记录可解析
  → 用与 goal 无关的输出代理 `_comp_off_score()` 排序，默认前 40），并入 `POOL_COMP`。
  实测武器组件池：**2 → 25+ 项**（Sam：剑 c20 + 斧 c21，`k ≤ 73`）。

### ③ ★ 顺带发现：**双槽混装比双槽同款更好**（搜索做不到，因为池里只有 2 项）
在搜索方案上逐项实测（`k≤73` 且两槽都合法的 25 项）：
| 主手 | 副手 | 含减抗 | 相对搜索方案 |
|---|---|---|---|
| it2878 恶毒尖刺 | it2878 | 176,235 | +0.00% |
| **it2870 锯齿尖刺** | **it2878** | **178,486** | **+1.28%** |
| it2878 | it2870 | 178,486 | +1.28% |
| it2870 | it2870 | 171,985 | **−2.41%** |
⇒ **最优是混装**（两枚不同 WPS 组件），双槽同款反而差。相对存档 = 178,486/163,959 = **+8.86%**。

### ④ 自检 `[63]` 新增 7 条（守 #86/#87）
`_req_lvl` 读 `k` 不读 `l` ／ 武器组件池自动生成且 ≥20 项 ／ 池内每项**等级合法** ／
池内每项**槽位可取** ／ 最优件 `it2878` 在池内 ／ **等级不够的 `it8898` 不在池内** ／
`plan_legal.check_level` **查得出** `it8898`、**不误判** `it2878`。

## 11:18~ 用户问「搜索跑太久，瓶颈在哪」→ 性能归因（陷阱 #88）

### 现场
补跑的那次 `--extreme --comp-topn 64` 跑到 **18+ 分钟**仍未出产物 ⇒ 手动停掉。
停时统计：**33 进程 / 累计 36,089 s CPU = 10.02 CPU-小时**。

### ★ 关键实测数字（机器空闲时）
| 项 | 值 |
|---|---|
| 单次 `plan_dps.dps_of` | **3.68 ms**（273 次/秒）⚠ 之前在 32 进程打满时测到 28 ms，**被夸大 7.6×** |
| 其中 `final_report` | ~70%（纯 Python 字典热循环：单次 ~16,000 次 `dict.get/setdefault`、153 次 `parse_mm`、1,762 次 `re.match`） |
| 其中 `load_char` | ~21%（每次换装重折叠 14 槽 + 重算套装/`skill_plus`/转化/`skill_mods`） |
| 单链固定开销 | ~32 s（建池 23.1 s + 满抗 ILP 31.9 s） |
| LNS | **126 次真实评估/轮**；30 轮串行 **13.7 s** |
| LNS 30 轮 + `--procs 16` | **1.8 s** ⇒ **7.6×**，结果逐位一致（167,681） |

### 归因（贡献排序）
1. **32 条链各算一遍「候选池 + 满抗解」**（与扰动无关，纯重复）≈ 32 × 30 s = 16 CPU-分钟。
2. **贪心 `local_search`**：单槽 `max_per_slot`(200)×14×`passes`(4)=11,200 ／
   双槽 C(14,2)=91 × `pair_k²`(4×4=16) × 4 = 5,824 ⇒ ~17,000 次/链。
3. **LNS** `max_iters`(400) × 126 = 50,400 次/链 ⇒ 32 链 161 万次。
4. ★★ **迭代内那 126 次评估在 `--extreme` 下全部串行** ——
   `--procs`（迭代内并行）与 `--chains`（链级并行）**互斥**，而 `--extreme` 走链级
   ⇒ 并行效率只有 1/32（每条链内部完全串行）。**这是最大的、可零改动修掉的一项。**

### ★ 本次「突然慢 2.5×」的直接原因（我自己的改动）
武器镶嵌物池 **2 → 25 项** ⇒ 武器槽可行组合 2²=4 → 25²=625（截断在 `eval_cap=240`）
⇒ 每轮更容易吃满上限 ⇒ 每链评估数 ×2.6 ⇒ 墙钟 7 min → 18+ min。
**教训：扩大候选池必须同时收紧「每轮评估上限」。**

### 优化清单（已写入 pitfalls #88）
O1 `--procs 16` 单链（零改动、已验证 7.6×）／O2 放开 `chains × procs ≤ 物理核`／
O3 候选池与满抗解只算一次、链间共享／O4 **自适应 `eval_cap`**（按代理分衰减定 N）／
O5 `parse_mm`/`_at` 字段名级正则缓存／O6 `load_char` 增量折叠／O7 无改善即停 + 削减预算。

### 附带修掉的一个自检坑
`tools/selftest.py` 里 `__import__("gd.DB")` 失败 —— 磁盘上是 **`gd/db.py`（小写）**，
`from gd import DB` 靠 **Windows 大小写不敏感**才能用；`importlib`/`__import__` 按名查会
`ModuleNotFoundError`。已在 `[63]` 里改用 `from gd import DB as …` 并注明。
`selftest` **全绿**（[63] 8 条）。

## 11:45~ 优化器性能重构（用户：「全部优化」「这时间也太久了」）—— 陷阱 #89 / #90

### ① 用户不满的直接原因（我造成的）
武器镶嵌物候选池 2 → 25 项后，武器槽可行组合 2²=4 → 25²=625（截断在 `eval_cap=240`）
⇒ 原本几乎免费的迭代现在每轮吃满上限 ⇒ 每链评估数 ×2.6 ⇒ 墙钟 7 min → **18.8 min**。
**教训：扩大候选池必须同时收紧「每轮评估上限」。**

### ② 而我自己的**测量方法也错了**（更要紧）
我拿 `gd auto` 整链路做 A/B 对照 —— 那条路有 ~55 s 固定开销（建池 2.3 s + 满抗 ILP 18.6 s
+ 报告子进程）会把被测差异淹没，**白跑三轮、耗掉好几分钟**，正是用户抱怨的那段时间。
⇒ 改成**只测改动的两个函数**（`tools/_bench_core.py`，跑完即删）：**6.6 秒拿到结论**。

### ③ 真正的瓶颈：**并行效率 = 1/32**（不是单次计算）
| 因子 | 实测 |
|---|---|
| 单次 `dps_of` | **3.81 ms**（262 次/秒）⚠ 之前在 32 进程打满时测到 28 ms，**夸大 7.6×** |
| 单链评估次数 | ~11.6 万（贪心 ~1.7 万 + LNS 400×240=9.6 万） |
| **并行效率** | **1/32** ★ 根因 |

根因：`--procs`（链内邻域并行）与 `--chains`（链级并行）被**写死互斥**
（旧注释理由「N×procs 会超物理核」）⇒ `--extreme` 在 32 核机器上跑的是
**32 条链 × 每条链内部完全串行**，每轮那 126~240 次互相独立的邻域评估**一次都没并行**。

### ④ 实施（四项）
1. **统一分配器** `autobuild._alloc_parallel(phys, chains, procs)`：总进程 = **物理核**，
   保证 `chains × procs ≤ 物理核`。默认 `procs = min(物理核, 4)`、`chains = 物理核 // procs`
   （16 核 ⇒ **4×4**）。显式 `--chains`/`--procs` 仍优先。
2. **贪心也并行**：新统一入口 `tune_dps.eval_trials(trials, real_dps, par, memo, slots)`，
   `local_search` 的单槽（每槽最多 200 候选）与双槽（槽对 × pair_k²）阶段**整批**走进程池。
   旧版只有 LNS 并行，而贪心占单链 ~1/3 时间。
3. **单次评估 −14%**（P2）：`parse_mm` 字段名分类永久缓存 `_MM_CLS`（原每次评估 ~1,300 次
   `re.match`）；`fields_with_buff` 记忆化（单次评估调 ~230 次、重复率 ~4×；
   ⚠ 必须返回**浅拷贝** —— 有调用点会往里注入 `skillChanceWeight`）；
   归因账本 `GD_ATTRIB=0`。
4. **收敛开关**：`--lns-patience N`（连续 N 轮无改善即停）、`--lns-proxy-tol`
   （代理窗口截断）、`--time-budget S`（墙钟硬预算，含建池与满抗搜索）。**默认全关 ⇒ 零漂移**。

### ⑤ ★★ 踩到并修掉的硬坑：**daemon 进程不能有子进程**（陷阱 #90）
第一次跑 `--chains 2 --procs 8` **整轮搜索静默挂死** —— 33 个 python 全在 0~2% CPU 空转。
traceback 给出确证：
```
AssertionError: daemonic processes are not allowed to have children
```
`multiprocessing.Pool` 的 worker 是 **daemon**，而链内要再建邻域池（`ParEval`）⇒ 被拒。
修法：链这一层改用 **`ctx.Process(daemon=False)` + `Queue`**，并补三件收尾：
① `Queue.get(timeout=5)` + 「所有子进程都不存活却没收齐 ⇒ 跳出」（防无限等）；
② `join(timeout)` 后 `terminate()`；③ worker 里 `except BaseException` 把 traceback 回传。

### ⑥ 实测（全部落盘可复跑）
**微基准**（同进程、同参数、同存档）：
| 阶段 | 串行 | 并行 8 | 加速 | 结果 |
|---|---|---|---|---|
| 贪心 `local_search` | 3.0 s | **0.5 s** | **6.2×** | 559,829 ↔ 559,829 **Δ0** |
| LNS `lns_search` | 1.6 s | **0.4 s** | **4.3×** | 536,038 ↔ 536,038 **Δ0** |

邻域并行加速比：2→1.9×｜4→3.3×｜8→6.8×｜16→10.2×（**递减**，故默认 procs 封 4）。

**端到端 `gd auto Sam --extreme --goal super`**（16 物理核，满抗 1070/1070）：
| 架构 | 链 × 进程 | 墙钟 | 结果 | 相对存档 |
|---|---|---|---|---|
| 旧 | 32 × 1 | **1128 s（18.8 min）** | 176,235 | +7.5% |
| 新 `--chains 4 --procs 4` | 4 × 4 | **113 s** | **175,726** | **+7.2%** |
| 新 `--chains 2 --procs 8` | 2 × 8 | **72 s** | 174,893 | +6.7% |
⇒ **10× 提速、质量仅少 0.3%**。产物 `data/plans/pivot/asc_v2b.json`。

### ⑦ 零漂移验证（硬指标）
`GD_ATTRIB` 未设（报告口径）与 `=0`（搜索口径）下 `dps_of('Sam')` 的
面板 / 实战 / 含减抗 / OA / DA **逐位相同**（81,363 ｜ 98,873 ｜ 163,959 ｜ 2,092.2 ｜ 1,404.5
= 锚点 v14）。报告子进程由 `_report_env()` 显式 `GD_ATTRIB=1`（否则归因表整块空掉）。

### ⑧ 改掉 5 条**把旧架构写成契约**的过时断言
`autobuild 默认并行度 = 逻辑核`（×2 处）、`--procs 与链级并行互斥`、
`并行路径先吃 memo`（实现搬进 `eval_trials` 了，`_memo.get(k)` → `memo.get(k) for k in keys`）。
新增守卫：`_alloc_parallel` 存在且 `chains × procs ≤ 物理核` / `pr.daemon = False` /
「子进程静默死掉」的收尾 / `eval_trials` 统一入口。
★ 踩到 `assert` 后**未写盘**（第一个替换成功、第二个失败 ⇒ 整批没落）——
改用**按行定位+切片**才稳（精确多行匹配在转义/空白上很脆）。
**`selftest` 全绿**；`sync_live` 全部一致；存档指纹 `ddb54cc16610` **未变**。

### ⑨ 一个**被我自己证伪**的猜测（记下来免得再犯）
我按 cProfile 的 `dict.setdefault` 计数（5,321 次/评估）推断「归因账本」是最大可省项，
实现 `GD_ATTRIB` 开关后实测**只省 1%** —— 那 5,321 次主要来自 `dmg.py` 的累加器，
账本本身只 ~16 次。开关留着（零成本、零漂移）但不指望它。
**教训：cProfile 的调用计数不能直接当「归因」，要实测开关后的差值。**

## 12:20~ 「能继续优化吗」+「后台卡一大堆没占用的 py 进程」→ 陷阱 #91 + 三个零代码旋钮

### ① 用户报的「空闲 py 进程堆积」—— **真实泄漏，我这次重构引入的**
进程树是**两层**：`gd auto`（父）→ 链进程 ×chains（**非 daemon**，因为链内还要建邻域池）
→ `ParEval` worker ×procs。两个缺陷叠加：
1. **`Process.terminate()` / `taskkill /F /PID`（不带 `/T`）只杀一个进程** ——
   Windows 上子进程**不随父退出而结束**（不像 POSIX 进程组）⇒ 父被强杀后整棵子树变孤儿。
2. **链进程内的 `ParEval` 从没被显式关闭** —— 正常结束靠 `concurrent.futures` 的
   `atexit` 兜底能回收，但**一被强杀 `atexit` 就不跑** ⇒ 每链 `procs` 个 worker 全留。
   （我此前用 `taskkill /F /PID` 清卡死进程，正好制造了这批孤儿。）

**修法三道**：① `_chain_worker` 的 `try/finally` **显式 `par.close()`**；
② 收尾一律 **`_kill_tree(pid)`**（`taskkill /T` 或 psutil `children(recursive=True)`），
**彻底删掉 `pr.terminate()`**；③ 新增 **`tools/kill_orphans.py`**（第四道保险 + 运维工具）：
用 **ctypes Toolhelp32** 查进程树（本机 `wmic` 不存在、Windows 图形壳命令工具静默失败、
bash 调它又被安全策略挡 ⇒ ctypes 是唯一可行路径），判据只有「**父进程已死**」一条，
**默认只列不杀**、`--yes` 才树杀。
**验证**：跑完 `--extreme` 后立刻检查 ⇒ **python 进程 0 个**。

### ② 三个**零代码**旋钮（实测）
| 配置 | 墙钟 | 结果 | 相对存档 |
|---|---|---|---|
| `--extreme`（旧预设） | 113 s | 175,726 | +7.2% |
| `--ilp-gap 1e-2` | 102.8 s | 175,726 | +7.2%（省 10 s，**结果相同**） |
| `--lns-patience 120` | 78.9 s | 174,893 | +6.7%（早停，略损） |
| **`--lns-cap 120`** | **67.8 s** | **175,726** | **+7.2%** ★ 白赚 −40% |
| **新预设（`lns_cap=120`）** | **80.1 s** | **175,726** | **+7.2%** |

★ **`--lns-cap` 240 → 120：墙钟 −40%、结果逐位相同** —— 邻域按代理降序排好后，
**前 120 个已覆盖全部真正竞争者**。已写进 `--extreme` 预设（显式 `--lns-cap N` 仍优先）。
⇒ `--extreme` 累计 **1128 s → 80.1 s ≈ 14×**，结果不变（175,726 / +7.2%）。

### ③ 单次评估的成本分布（**包装计时**实测；别再靠 cProfile 猜）
| 段 | 耗时 | 占比 |
|---|---|---|
| `load_char` | 0.62 ms | 19% |
| `final_report` | 2.31 ms | 71% |
| 其余（attrs/rr/wps） | 0.32 ms | 10% |

`final_report` 内部函数级：**`dmg.convert` 1.24 ms（76 次/评估）**，其内
`_apply_one` **920 次**、`_conv_matches` **1,684 次**、`_targets` 316 次；
`skill_group` 0.22 ／ `parse_mm` 0.18。
⇒ **转伤链占单次评估 ~30%**，是下一块肉（预编译「来源类型 → 目标类型」表，
把 1,684 次 `_conv_matches` 换成查表），**上限约 −15%**。

### ④ ★ 被实测否掉的猜测（第二次了，必须记住）
我原以为「只依赖 `levels` 的聚合」（`sk_pct` / 光环平伤 / `skill_children` /
`enemy_da_cut`）是最大可省项（装备搜索时 `levels` 恒定 ⇒ 完全可缓存）。
**实测只有 0.23 ms = 7%**，不值得做。
（第一次是「归因账本」，实测只省 1%。）
**cProfile 的调用计数 ≠ 归因；猜出来的「最大项」必须用开关差值验证。**

### ⑤ 新增守卫 4 条（`selftest` 全绿）
清理一律走 `_kill_tree`（且 `pr.terminate()` 必须**不存在**）／链 worker 有 `finally` 关邻域池／
`--extreme` 预设 `lns_cap=120`／`kill_orphans` 存在且**默认不杀**。
★ 顺带修了一条**自己撞自己**的断言（它要求 `pr.terminate()` 存在，而那正是要删的）。
`sync_live` 全部一致；存档指纹 `ddb54cc16610` **未变**。

## 12:35~ 啃转伤链（用户「继续」）→ 陷阱 #92：−8.2% 单次评估 + **第三次归因错误**

### ① 实测出的结构（`gd/dmg.py::convert`）
| 项 | 实测 |
|---|---|
| `convert()` 调用 | **76 次/评估** |
| 步骤实例 | **152 个/评估** |
| **唯一步骤签名** | **1 个** ⇒ 理论复用 **456×** |
| 源数/convert ｜ 规则/步骤 | 5.5 ｜ 2.0 |
原因：同一次 `_make_hit` 里「主路径 1 次 + 分来源账本 5 组」用的是**同一份**转化配置。

### ② 改法（`gd/dmg.py`，算术一行没动）
把每步规则预编译成「来源类型 → (目标表, 总量)」= `_rel_tbl(step)`，
**按步骤签名全局缓存**（真实负载下只有 1 个签名 ⇒ 编译 1 次/评估）。
- 未知来源类型**就地补算写回**（行为不变）／不可哈希 ⇒ **不缓存照旧算**（零风险回退）／
  缓存上限 4096 超了 `clear()`。

### ③ 验证（零漂移是硬指标）
- **fuzz 4000 例**（随机来源 × 规则 × pierce/wd，含空步骤、`elemental` 源与目标、
  >100% 分摊、DoT）⇒ 与旧实现**逐位一致、0 例差异**；
- **同进程 A/B**（旧实现临时挂回 `DMG.convert`，同预热状态）：
  **3.310 ms → 3.039 ms = −8.2%**，含减抗 **176,235 ↔ 176,235 逐位一致**；
- 锚点 v14：面板 81,363 ｜ 含减抗 163,959 ✓；
- 自检新增 **`[64]`**：内嵌**旧实现的参考复刻**跑 240 例 fuzz + 4 条缓存契约断言。**全绿**。

### ④ 端到端（**诚实说明**）
改造后 `--extreme` = **82.1 s / 175,726（+7.2%）**，落在最近几轮 **67.8~82.1 s** 的噪声带里
（光 ILP 就在 13.4~18.6 s 跳）。
⇒ 只能声称**受控 A/B 的 −8.2%**，**不能**声称端到端快 8%。
**下一块真正的肉 = 固定开销 ~26 s（占 32%），其中满抗 ILP 独占 18.6 s。**

### ⑤ ★★★ 我第三次归因错误 —— 统一成一条方法论
先前用「包装计时」测出 `dmg.convert` = 1.24 ms、占单次 **30%**，据此说「转伤链是最大块」。
**那 1.24 ms 主要是给 2,920 个内部调用打包装的开销**（`_apply_one` 920 +
`_conv_matches` 1684 + `_targets` 316），不是 `convert` 本身。真实收益 8.2%。
**⛔ 包装计时只能找「谁被调用得最多」，不能当耗时占比**（包装 ~0.4 µs/次，
几千次就是毫秒级，会把小函数吹成大热点）。**准确占比只能靠同进程开关 A/B。**
（前两次：cProfile 的 `setdefault` 计数 → 误判「归因账本」；包装计时 → 误判「转伤链」。）

### ⑥ 顺带踩到的一个转义坑
heredoc（`<<'PYEOF'`）里写 `print("\n[64] …")`，经 JSON + Python 两层解析后
**变成了真换行** ⇒ `SyntaxError: unterminated string literal`。
修法：**拆成 `print()` + `print("…")`，彻底不用转义**。

## 12:38~ 啃「固定开销」里的满抗 ILP（用户「继续」）→ 陷阱 #93：**变量 −66%、求解 −62%、解逐位不变**

上一轮留的坐标：`--extreme` 77.6 s，固定开销里满抗 ILP 独占 13.7 s
（建池 1.27 + 建矩阵 0.11 + **HiGHS MIP 10.98**）。这就是本轮的目标。

### ① 先归因，不猜 —— 建了个**同进程参数矩阵**台架
★ 关键：**不能用 `tools/ilp_res.py` 的 CLI 复现对比** —— 它的 env 与 `gd auto` 不同
（少 `GD_ENEMY_RES_PROFILE` / `GD_REAL_GOAL` / `GD_ATTR_BUDGET`），候选池小一圈
（35,664 vs 38,267 变量），数不可比。**我第一次就踩了**（还把 MIP 跑成 `Not Set`）。
正解：写个 harness `import autobuild` 后 **monkeypatch `IR.Model.solve`**，
在真实路径第一次求解时把整个矩阵跑完 —— 同进程、同 candmap、同 HiGHS。

| 配置 | 求解耗时 | 结果 |
|---|---|---|
| gap 1e-3 / 5e-3 / 1e-2 / 3e-2 / 1e-1 | 9.5 ~ 10.2 s | **全部 Optimal、目标 3792.586、解逐位相同** |
| `mip_start`（喂最优解回去） | 8.43 s | 同解，但要先花 9 s 拿到它 ⇒ 净亏 |
| `mip_heuristic_effort=0.2` | 9.50 s | 同解，无收益 |
| `mip_detect_symmetry=True` | 3.48 s（剪枝后） | 无收益 |
| `presolve=off` | 6.26 s | 目标同但**解不同**（另一个最优）⇒ 弃用 |
| ★ **(抗性, 目标) 支配剪枝** | **3.45 s** | **目标与解逐位相同** |

⚠ **纠正上一轮的一个说法**：`--ilp-gap 1e-2 省 10 s` **是噪声**。HiGHS 是
**证完最优性**才停（MIP gap 归零），gap 放到 1e-1 也一秒不省。
（又一次印证：**端到端 67.8~82.1 s 的噪声带里，小于 15 s 的差异都不能当结论**。）

### ② 判据（为什么精确）
A 支配 B ⇔ ① `res_A ≥ res_B` 逐维 ② `obj_A ≥ obj_B` ③ **不许「A 双手替非双手 B」**
（否则凭空多出「副手必须为空」的约束）。⇒ 任何解里的 B 都能就地换 A（抗性只增不减、
目标不降）⇒ 最优值不变。★ 比 `gd/opt._skyline`（要求输出**每一维**不劣）**严格更强**。

实现两处细节：
- **快速路径 `(ri[:ndom] > runmax).any()` 只影响「剪得多不多」**：命中 ⇒ 该维刷新纪录
  ⇒ 不可能被支配 ⇒ 直接留。误判成「留」只是少剪，**永不误删**（判据方向是「留 ⊇ 真前沿」）。
  初版用 `np.array(KK)` 每次重建，2.53 s；改成**预分配 K + runmax** 后 **0.63 s**。
- **副手 ∅ 强制保留**（`fixed_first`）：双手约束 `x[主手2H] ≤ x[副手∅]` 全靠它。

### ③ 落地 + 实测
`tools/ilp_res.py` 新增 `_dom_core` / `_dom_key` / `dom_reduce`（**内容指纹落盘缓存**），
`cap_search` 里已接；`GD_ILP_DOM=0` 或 `--no-dom` 可关。CLI 也加了 `--no-dom`。

| 项 | 前 | 后 |
|---|---|---|
| 变量 | 38,267 | **13,034（34.1%）** |
| 逐槽 | 主手 3224→**84** ｜ 副手 3230→**85** ｜ 戒指 1571→483 ｜ 圣物 23→10 | |
| 剪枝自身 | — | 0.63 s 首跑 ／ **0.00 s 缓存命中** |
| MIP | 9.65 s | **3.70 s** |
| ILP 小计 | 13.7 s | **7.9 s**（热 7.3） |
| `--extreme` 全程 | 77.6 s | **68.0 s（−12.4%）** |
| 结果 | 175,726 ｜ +7.2% | **175,726 ｜ +7.2%（逐位相同）** |

**对照组**：`--extreme --no-dom` = ILP 14.3 s / 全程 **79.1 s** / 同样 175,726。
⇒ 同一晚四次运行：R1默认 68.0 ／ R2默认(热缓存) 71.8 ／ R3 `--no-dom` 79.1 ／
上一轮基线 77.6 —— **开关差 ~11 s 站在噪声带之外**，可称。

### ④ 验证（零漂移是硬指标）
- 同进程矩阵里**剪枝解 == 全模型解**（逐位）；
- 端到端 ILP 目标 3792.59（= 改造前 3792.5855）、紫2 蓝8、DPS 175,726 全不变；
- 自检新增 **`[65]`** 八项：合成实例上「朴素 O(n²) 判据保留的**必须全被保留**」、
  **全模型 vs 剪枝模型 MIP 最优值逐位相同**、剪枝解未凭空造件、∅ 永不删、
  缓存 key 契约（同输入同 key／换权重换 key）、`cap_search` 已接且可关。
  ★ 第一次写的时候我用了个不存在的 `np_order65`，且 `proxy_of` 在 fake O 上没有 `ev`
  —— 自检**当场报错**（而不是静默通过），这就是守卫的价值。
- **`tools/selftest.py` 513 项全绿**；`sync_live` 全部一致；存档指纹 `ddb54cc16610` **未变**。

### ⑤ 顺手修掉一个**会撒谎的自检**
`ilp_res.proxy_of` 复核目标时只累加 `(b, c, a)`，**漏了 `slot_cands` 折进去的
最优词缀 `p` / `sf`** ⇒ 打印的「代理复核」系统性偏低（3437.55 vs 3427.39，
差 ~10 = 词缀贡献），一直显示「与求解器目标应一致」却不一致。
修：`for gid in (b, c, a, p, sf)`。**这类「永远对不上却没人管」的自检比没有更危险。**

### ⑥ 归位
- 归因台架从临时脚本升格为 **`tools/bench_ilp_opts.py`**（spawn-safe：`if __name__`
  守卫必须写，否则子进程会当 `__mp_main__` 再跑一遍 `AB.main()` —— 这次差点被输出锁拦住）；
  三个 `_bench_ilp*.py` 临时文件已删。
- 文档：`docs/pitfalls.md` **#93** ／ `SKILL.md` 摘要 ／ `docs/commands.md`
  （含纠正 `--ilp-gap` 的旧说法）／ `autobuild.py` 的 `--no-dom` 与 `--ilp-gap` help 文案。
- 锚点 **v14 不变**（模型没动，只动求解器的候选裁剪）。

## 14:19~ 「完整测一次：75 级 士兵+夜刃 的职业最优解」→ 陷阱 #94（建池 O(n²)）+ #95（三处写死的默认值）

用户要求：完整测一次 **lv75 战士+夜刃（Soldier+Nightblade）** 的职业最优解。
存档只有 `_xyf`（**lv76**、classids [1,4]、专精各 50/50、103 条技能加点）；
确认口径后按 **存档原样（lv76）** 跑，深度 = **四轴全跑 + 伤害循环文档**。

### ① 用户中途报「怎么这么慢　只有一个在跑」（附截图：单 python 3% CPU）
先量再改。任务管理器那 1 个进程 = 32 逻辑核里的 **1 核**；`tools/kill_orphans.py`
采样确认「24 个 python 里只有 1 个在算（3.00% CPU、320 MB）」，其余是空壳 launcher。
日志停在「[减抗技能] …」之后 —— **卡在建池**，日志一小时后才出现这一行：

```
[满抗搜索] 候选 头部 12600 ｜ 项链 6285 ｜ 胸甲 10481 ｜ … ｜ 副手 3740
[满抗搜索] 建池 419.5 s          ← 真凶
[ILP 支配剪枝] 1.28 s ｜ 变量 62743 → 20366
[ILP] Optimal ｜ 目标 3656.69 ｜ 16.93 s        ← 只占 4%
```

**根因（陷阱 #94）**：`gd/opt.py::_skyline` 对每个候选扫**全部 n 行** ⇒ **O(n²)**；
lv76/`--extreme` 单槽原始候选高达 **61,510**（150×24×14），单槽 ~110 s、14 槽 **419.5 s**，
`gd auto` 全程 **546 s（9 分 06 秒）**，且**纯单线程**。
★ 从缓存反推原始规模的办法：`data/cache/prune/*.pkl` 里 `keep` 的**长度** = 原始候选数。

### ② 改法（三条，结果**逐位不变**）
① 只与**已保留**行比（支配有**传递性**：被丢的 j 若能支配 i，支配 j 的那行也支配 i）
② **字典序降序**预排序（`np.lexsort`）⇒ 保证支配者排在被支配者**之前**，① 才等价
③ `(ri > runmax).any()` **快速路径** + 512 行**分块、命中即停**
`GD_SKYLINE=slow` 回退 A/B；自检新增 **`[66]`**。

| 项 | 前 | 后 |
|---|---|---|
| 单槽 `_skyline`（61,510 行） | ~110 s | **1.3 s** |
| **建池** | **419.5 s** | **21.9 s（19.2×）** |
| **`gd auto --extreme --with-weapon` 全程** | **546.1 s** | **147.5 s（3.70×）** |
| 逐槽候选数 / ILP 目标 / 真实 DPS | 12600…3740 ／ 3656.69 ／ 200,754 | **逐个相同** |

★★ **差点带着一个 bug 上线**：第一版写 `blk = K[a0:a0+512]`，而 `K = np.empty_like(rows)`
—— **越界切片读到未初始化的行**，垃圾值凑巧 ≥且> 当前行就**误删真候选**。
非负数据上完全不显形（零恰好是最小值），**只在含负值的数据上暴露**：
fuzz `('rand', 1200, 7)` 三组种子全部 441 行不一致。修法 `min(a0+512, cnt)`。
**教训：`empty_like` 当缓冲区，任何切片都要夹到 `cnt`；而且自检里必须有一个「与旧实现逐位对拍」的 fuzz。**

### ③ 四轴发现工具层的三个坑（陷阱 #95）
第一次四轴跑出来的第一行是「起点 评分 12736.0 ｜ 形态 **werewolf**」——
而 `gd auto` 同一角色报 **200,754**，**差 15.8 倍**。三个写死的默认值叠加：
1. `plan_dps` / `tune_attrs` 形态兜底写死 `'werewolf'`，而 `guess_arch` **只认变身技能**
   ⇒ 非变身角色（士兵+夜刃）一律被判成狼人；`gd auto` 走的却是 `pick_archetype` 职业组合匹配；
2. `tune_skills` / `tune_devotion` 的 `PLAN` 默认是**写死的 Sam 文件**、`ARCH` 默认 `wolf_nightblade_fast`；
3. `coordinate_ascent` **根本没把 PLAN 传下去**。
★ 最坑的是**不报错** —— 面板 89,217 / 评分 113,736 / 星点 58/58 全是「看着很合理」的假数。
修：新增 `plan_dps.resolve_arch()`（**guess_arch 在前** ⇒ Sam 仍得 `werewolf`，**零漂移**）
+ `arch_by_mastery()`（与 `autobuild.pick_archetype` 同源、带记忆化）；`tune_attrs` 三处兜底改掉；
`coordinate_ascent` 加 `_resolve_plan()` + 显式 `env['PLAN']` + arch 定死后下传；
顺带补 `--plan` / `--gear-extreme`（装备轴默认强度≠主线强度，会看着像「装备轴没用」）。
修后 `_xyf` 面板 **63,424**，与 `gd auto` 的「现状」逐位吻合。自检新增 **`[67]`** 八项。

### ④ 交付（`_xyf` lv76 士兵+夜刃 · 双持物理）
- 全链路：`gd auto _xyf --extreme --with-weapon` → **覆盖 1070/1070 ｜ 真实 DPS 200,754
  （现状 63,424，+216.5%）** ｜ 紫9 蓝2 绿3 ｜ 主副手各一把「蝎尾狮长剑」（绿、奇术师/圣贤、磐石/虚灵屏障）
  产物 `data/plans/xyf_auto.json` + `xyf_auto.md`
- 四轴（逐轴单独跑，各自出报告 `data/plans/final/ASCENT_xyf_*.md`）：
  | 轴 | 最优性 | 结论 |
  |---|---|---|
  | 属性点 | **全局最优**（2,211 组合全枚举 / 4 s） | **+0.00%**（已是全局最优，加点 10/66/1） |
  | 技能点 | 局部最优（1-1 换位） | 自报 +11.62%，**但工具打红字「默认攻击权重 = 0 ⇒ 主输出消失，方案不可用」**（WPS 撑爆）⇒ 未采纳 |
  | 星座 | 启发式 | **+0.00%**；且「亲和力自洽：**否** —— 无名士兵 缺 Ascendant 15（实得 14）」 |
  | 装备 | 启发式 LNS | **+216.50%**（= 主线那次） |
- 伤害循环文档：`data/plans/final/CYCLE_soldier_nightblade.md`（1,356 行）
  面板 146,635 ｜ 实战 154,657 ｜ 对怪 200,754 ｜ 过甲 200,756

### ⑤ 两个待用户决策的异常
1. **`_xyf` 是改过的存档**：点亮星座节点 **58 个** > 游戏上限 **55**。星座轴亲和力自检不过
   大概率源于此 ⇒ 这角色的星座/点数结论要按「存档可能非法」来读。
2. **技能轴 +11.62% 不可信**：它是靠把 WPS 权重推到 ≥100、默认攻击权重归零换来的，
   工具自己标了「方案不可用」。要不要改模型口径（区分 WPS 撑爆 vs 真增益）需先批准。

## 15:05~ 用户问「跑的时候这么多进程是在干嘛」（附截图：15 个 python @147 MB）→ `tools/proc_tree.py`

先查现状：**当时 0 个 python 在跑**（截图是刚才那次运行中途截的）。于是**起一次真运行采样**，
而不是照代码猜。★ 用 `&` 起后台**被沙箱回收**（memory 里记过：工具调用结束即回收进程树）
⇒ 必须用 Bash 工具的 `run_in_background`。

### 实测（`gd auto _xyf --extreme --with-weapon`，默认 `--chains 4 --procs 4`）
| 角色 | 个数 | 小计核 | 均核 | 均内存 | 在干什么 |
|---|---|---|---|---|---|
| 父 `gd auto` | 1 | 0.00 | 0.00 | 382 MB | 建池 → 满抗 ILP → 收链结果 → 出报告 |
| **链进程** | **4** | 0.26 | 0.06 | 359 MB | 各自**独立跑完整一轮**（贪心 + LNS），取最优 |
| **ParEval worker** | **16** | **14.19** | 0.89 | 160 MB | 链内**邻域评估并行**（每个 = 一个打分进程） |
| 启动壳 | 5 | 0.00 | 0.00 | 11 MB | bash/沙箱包装，不干活 |
| **合计** | **26** | **14.44** | — | — | ≈ **16 物理核跑满** |

⇒ 截图里那 15 个 `3.0% CPU` 的进程 = **16 个 ParEval worker**（3% × 32 逻辑核 ≈ 0.96 核/个，
与实测 0.89 核吻合）。**这不是异常，是把 16 个物理核喂满的正常形态。**

### 内存为什么 ×21
实测单进程构成：**裸 python 15 MB** → **`import gd.opt` 111 MB**（离线物品/词缀/技能库）
→ 建完候选池 117 MB（`--extreme` + 14 槽 → ~147 MB）。
Windows 用 **spawn 不是 fork** ⇒ 每进程各自重载一份库，**内存不共享**；
21 × 147 MB ≈ 3 GB 是**固有代价**，不是泄漏。

### ★ 两个「只有一个在跑」不是同一件事（用户两次都问到这个）
- **建池阶段**：只有父进程 1 个核 → 陷阱 **#94**（`_skyline` O(n²)，419 s → 22 s）。
- **搜索阶段**：1 + 4 + 16 全铺开 → 就是这次看到的。已写进 `docs/commands.md`。

### 新增工具（踩了两次才判对角色）
`tools/proc_tree.py` —— 采样进程树并按角色打印。**角色判定不能用绝对深度**：
外面还套着 **3 层 ~5 MB 的启动壳**（17400→33012→47008→真的 `gd auto`），
按「后代最多」会把根判到壳上（第一版就这么错了，输出 1/1/1/1 + 22 个「未归类」）。
正确判据：**有 python 子进程 且 常驻 ≥ 80 MB**（壳都 < 25 MB，真进程必 ≥ 110 MB）⇒
在其中取后代最多者为父。第二版实测 1/4/16/5 完全正确。
（`run_in_background` 起的 3 次探测全部 `--out /tmp/...`，**没碰正式产物** `xyf_auto.json`。）

## 15:26~ 「现在一轮 LNS 大概多久」→ 先加计时（原先完全看不见），再受控实测

日志里**根本没有逐轮计时**（只有「改善时」打印一次累计秒），所以先给
`tools/tune_dps.py::lns_search` 补上 `_rt` 逐轮墙钟，结尾多打一行
`LNS 单轮耗时：均值 / 中位 / p90 / 均 N 次真实评估/轮 / 轮/秒`（纯日志，零漂移）。

**一轮的定义** = 一次 destroy & repair：随机放开 `k∈{2,3,4}` 个槽 → 枚举 `topk^k`
→ 抗性需求掩码 → 代理降序取前 `eval_cap`（`--extreme`=**120**）个算**真实 DPS** → LAHC 判定。

### 实测（`_xyf` lv76 / `--extreme` / 固定 12 轮，同进程同池）
| 配置 | 均值 | 中位 | p90 | 均真实评估/轮 | 轮/秒 |
|---|---|---|---|---|---|
| 1 链 × **1 进程**（串行） | **441 ms** | 696 ms | 750 ms | 81.1 | 1.6 |
| 1 链 × **4 进程**（生产） | **178 ms** | 295 ms | 300 ms | 81.1 | 2.6 |

⇒ **生产配置一轮 ≈ 0.2~0.3 s**（串行 0.44~0.70 s）。
拆开看：81 次真实评估 × ~2.6 ms ≈ 210 ms ≈ **2/3 的轮时间**，其余是 numpy 邻域生成 + 进程池 IPC。
★ 注意**均值 < 中位**（少数轮次邻域特别小）⇒ **外推总时间要用均值**：
400 轮 × 178 ms ≈ 71 s，与观测到的「链完成」总耗时吻合（该次 `--extreme` 全程 **106 s**、
含建池 22 s + ILP 21 s）。

### 顺带
`--procs` 从 1 → 4：**6.464 ms/次 → 2.577 ms/次**（`ParEval` 通报值），轮时间 −60%。
`--extreme` 总轮数 = **400 轮/链 × 4 链 = 1600 轮**。自检 524 项全绿。

## 15:44~ 「测完整一轮从0构建的时间：75级 亡灵狂战士 不参考存档」→ 陷阱 #96 + 新工具 `make_zero_char.py`

用户补充口径：**「模型要直接拿到最优解，而不是询问形态；当然也要支持设定形态」**。

### ① 先查阻塞（两个，都有证据）
- `{8,10}`（死灵 class08 + 狂战士 class10）在 `archetypes.json` 里**完全不存在**
  （带 8 的只有 `necro_inquisitor(8,7)`，带 10 的全是 `(10,4)`）⇒ `pick_archetype` 按重叠度乱猜。
- `gd recipe`（唯一的「三要素→完整 BD」入口）**断链**：实跑第③步就崩
  `can't open file '...\data\gd_opt.py'`（它还依赖 `bd_gen.py`，都已被现有工具链取代）。
  ★ 但「①预算 ②配方」两步还有效：`gd/alloc.*_budget(lv)` ← `data/level_table.json`，
  **lv75 = 技能 210 / 属性 84 / 虔诚 55**。

### ② 补形态（3 个）—— 写进**增补文件**
`necro_berserker`(human) / `wolf_necromancer`(werewolf) / `raven_necromancer`(wereraven)，
`core_skills` 按既有形态同一口径**机械派生**，`damage_weights` 故意不写（不预设伤害方向）。
★★ **必须写 `data/archetypes_gdskill.json`** —— `data/archetypes.json` 被 migrate 无条件覆盖。
**我一开始没照做，代价见 ⑤。**

### ③ 新工具 `tools/make_zero_char.py`：造「从 0 角色」到临时目录
`gd/paths` 认 `GD_SAVE` 重定向 ⇒ 拿 `_Cx666`(lv28, class{8,10}) 当壳复制到
`data/scratch/zero_save/save/main/_ZERO75*`，按 block2 的**明文字段偏移表**定长改写：
level / attribute_points / skill_points / devotion_points / 三围 50.0 / 技能区只留 default+精通条。
★ **等级要改两处**：`doc['level']` 读的是**文件头**那个，只改 block2 的 `level_in_bio`
会「改了但没生效」（实测踩到）。`--learn` 可额外学形态技能。
★★ **安全闸**：第一版只比 `tgt == 真实目录`，于是 `--out <真实目录>` 会把目标落到
`<真实目录>/save`（在它**里面**）⇒ 闸门形同虚设。**自检的功能性验证当场抓到**并清理残留。
现在比的是**祖先/后代关系**。
✅ **真存档完好**：`main/` 下仍只有 `_1234/_236/_Cx666/_Sam/_xyf`，`_ZERO75*` 只在重定向目录里。

### ④ 关键发现：**从 0 角色上变身形态恒为 DPS 0**
人形态 34,424（全靠**装备授予技能**打）；狼人/鸦人 → **0.0**。
原因：形态门控按 `root_skills[0].granted` 裁技能栏，而角色**没学过 werewolf1/wereraven1**
⇒ 连装备 WPS 也被剔除 ⇒ 一个攻击都不剩。
**正解**：`tools/make_alloc.py <形态> <等级>` 先出加点 → `GD_SKILL_JSON` 注入。
⇒ **「形态自动决策」= 先按形态出加点 → 各形态跑一遍 → 取最优**（`sweep_arch.py`）；
`gd recipe` 里「③ 装备 ⇄ ④ 技能」的顺序是**错的**，技能加点必须先于形态比较。

### ⑤ 我踩的坑：跑 `merge_archetypes()` 把**只写在 archetypes.json 里的手工修复冲掉了**
`avalanche` 缺 `wpattack01/wpattack3`、`raven_nightblade` 缺 `form` —— **4 项原有守卫立刻报红**。
根因：2026-09-20 那次「avalanche 补 WPS」和 #78 那次「补 form」都**只改了 archetypes.json**，
没登记进增补文件 ⇒ 一合并就回退。修法：把两处修复**登记进 `archetypes_gdskill.json`** 再合并
（内容从守卫注释 + 日志原文完整复原）。自检 **531 项全绿**。

### ⑥ 实测：从 0 构建一整套（lv75 死灵+狂战士，16 物理核）
| 阶段 | 墙钟 |
|---|---|
| 造从 0 角色（`make_zero_char.py`） | ~1 s |
| 三形态加点（`make_alloc.py` ×3） | ~2 s |
| **形态普查（3 形态并发 24 进程 / LNS 400 轮）** | **118.1 s** |
| 赢家四轴（属性+技能+星座，2 轮） | **22 s** |
| **合计** | **≈ 143 s（2 分 23 秒）** |

- 单形态（8 进程）**97~118 s**；并发**重叠收益 2.79×**（串行和 329.8 s）。
- **最优形态 = 狼人 `wolf_necromancer` 51,402**（人 33,949 / 鸦 33,711）—— 模型自己选出来的。
- 四轴：属性 **全局最优 +0.00%**（1,485 组合 / 4 s）、技能 **+27.46%**（2 s）、
  星座 +0.00%（从 0 角色星点 0/0 ⇒ 得先「建」星座树，不是重排）。

## 16:50~ 「重做 recipe：尽可能 py 自动化、CLI 高一点、减少 agent 反复写原始代码」→ 陷阱 #97

### 设计原则（用户这句话直接决定了架构）
**「减少 agent 反复编写原始代码」⇒ recipe 必须是编排器，不能自己实现逻辑。**
v1 烂掉的根因就是它把六步流水线**复制**了一份（还 `cd data && python gd_opt.py`，
那两个脚本早就不存在）⇒ 底层一演进就烂在那。v2 三条：

1. **纯编排**：每阶段 = 对现有工具的**一次子进程调用**（一份实现，不存在两处漂移）；
2. **分阶段状态**：`data/plans/recipe/<key>/state.json`，成功阶段默认跳过、`--stages` 挑段、
   `--force` 全重跑 ⇒ 改中间一步不用从头再来；
3. **形态自动决策**：给职业组合 `8,10` 就自动展开**全部**形态并发跑取最优
   （回应上一轮「模型要直接拿到最优解，而不是询问形态」）；`--forms` 仍可收窄。

`gd/recipe.py` → **12 行薄壳**（`runpy.run_path` 转发到 `tools/recipe.py`），
CLI 入口 `python -m gd recipe` 不变。★ 这样**不用碰 `gd/cli.py`**（它在镜像对里，改要 `--capture`）。
v1 备份在 `/tmp/recipe_v1_backup.py`（424 行）。

### 实测：一条命令 99.2 s
`python -m gd recipe 8,10 75 --from-zero`：
S1 造从0角色（**自动学入 6 条形态技能**）0.1 s → S2 三形态加点 0.3 s →
S3 形态普查 3 形态并发 **76.1 s** → **wolf_necromancer 51,402 最优** →
S4 四轴 18.0 s（属性 +0.00%／技能 +27.46%／星座 +0.00%）→ S5 交付 5.0 s。
产物 `data/plans/final/RECIPE_8_10_lv75_zero.md`。
**对比我上一轮逐条敲命令手搓同一件事：143 s**，且每步都要现场拼命令行。
续跑 `--stages 4 --force` **只重跑 S4 = 17.1 s**。

### 三个实现坑
1. ★★ **续跑不能在循环里注入环境**：`GD_SKILL_JSON` 原先只在 S2/S3 后设 ⇒ `--stages 4`
   续跑会让四轴在**无技能**状态下评估（数字全错）。改成**进循环前从 state 读赢家+加点**。
2. ★ `--extra` 传 `-` 开头的值必须写 `--extra="--with-weapon"`，否则 argparse 报
   `expected one argument`。
3. ★ **交付要有硬判据**：S5 成败 = `build_report and cycle_doc` 都要有 ——
   伤害循环文档是铁律，缺了整段算失败，不许「报告出来就算过」。

自检新增 **`[69]`** 13 项（形态展开／`learn_for`／预算／编排器契约／循环文档硬交付／
`--list-forms` CLI／不存在的职业组合非零退出）。**总计 544 项全绿**，镜像一致。

## 17:02~ 「对比 狂战士夜刃怎么样 是提升还是削弱」→ 同口径跑 {10,4} + 陷阱 #98

### 先修一个会毁掉对比的坑
`make_zero_char` 原先写死 `--shell _Cx666`（class {8,10}）⇒ 拿它造 `{10,4}` 的角色时，
`--classes 10,4` 只是**新增**精通条，**壳原有的 8 号职业还留着**，`read_meta` 读出 `[8,10]`，
对比直接失效。修：`pick_shell(classes)` **按目标职业自动挑壳** + 写档时**先清掉壳里多余的职业**。

### 结果（完全同一套流程：lv75 / 从0 / 同预算 210·84·55 / `--extreme --with-weapon` / LNS 400 / proj=1 / 每形态 5 进程）
| 形态 | 死灵+狂战士 {8,10} | 狂战士+夜刃 {10,4} | 差 |
|---|---|---|---|
| human | `necro_berserker` 35,188 | `avalanche` **78,843** | {10,4} **+124.1%** |
| werewolf | `wolf_necromancer` **51,402** | `wolf_nightblade` 52,135 | {10,4} **+1.4%** |
| wereraven | `raven_necromancer` 33,711 | `berserker_nightblade` 51,397 | {10,4} **+52.5%** |
| **最优对最优** | 51,402 | **78,843** | **{10,4} +53.4%（{8,10} −34.8%）** |

### 判决与最关键的 nuance
- **按当前形态定义：`{10,4}` 更强 ⇒ 死灵+狂战士是削弱**（+53.4%）。
- ★★ **但狼人形态下只差 1.4%** ⇒ 强度几乎由「形态 + 它授予的技能」决定，
  **第二个职业在变身形态里几乎不出力**（形态门控剔掉第二职业的武器池）。差距集中在人/鸦两形态。
- ★★ **人形态那 2.2 倍很可能主要是「条目质量」不是职业强弱**：`avalanche` 是历史手工调过的
  （仅补 2 个 WPS 就值 +16.4%），`necro_berserker` 是我今天机械派生的。
- 两侧四轴余量不同且单位不同（{8,10} 技能 +27.46% / {10,4} 属性 +12.71%）⇒
  **这是「同一套自动流程下」的对比，不是「都调优到极致后」的终评**。

报告 `data/plans/final/COMPARE_8_10_vs_10_4_lv75.md`。自检 **546 项全绿**（新增 `pick_shell` 守卫）。
真存档仍是那 5 个角色，从 0 角色全在 `data/scratch/zero_save/save/main/`。
